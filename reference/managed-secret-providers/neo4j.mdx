---
title: 'Neo4j Aura'
description: 'Connect Neo4j Aura to Alter Vault for secure API access'
---

## Overview

Use Neo4j Aura credentials to make authenticated API calls through Alter Vault without exposing API keys in code.

| Property | Value |
|----------|-------|
| **Provider ID** | `neo4j` |
| **Category** | Database |
| **Credential Type** | Base64 Credentials |

## Step 1: Get Credentials

<Steps>
  <Step title="Log in to the Aura Console">
    Log in to the [Neo4j Aura Console](https://console.neo4j.io/).
  </Step>
  <Step title="Note the instance credentials">
    Create (or open) an instance and note the generated username and password — the password is only shown at creation.
  </Step>
  <Step title="Encode the credentials">
    Base64-encode the credentials: `echo -n 'neo4j:password' | base64` and paste the result into the credential field.
  </Step>
</Steps>

<Warning>
  Never share credentials publicly. Store them securely and only enter them in the Alter Vault Developer Portal.
</Warning>

## Step 2: Add to Alter Vault

<Steps>
  <Step title="Open the Developer Portal">
    Go to [portal.alterauth.com](https://portal.alterauth.com) and navigate to the application.
  </Step>

  <Step title="Add Neo4j Aura">
    Go to **Managed Secrets** > **Add Provider** > **Neo4j Aura**.
  </Step>

  <Step title="Enter credentials">
    Paste your Base64 Credentials into the credential field.
  </Step>

  <Step title="Save">
    Click **Save**. You'll receive a `grant_id` to use with the SDK.
  </Step>
</Steps>

## Using in Code

```python
response = await alter_app.request(
    HttpMethod.POST,
    "https://YOUR-INSTANCE.databases.neo4j.io/db/neo4j/query/v2",
    grant_id="YOUR_GRANT_ID",
    json={"statement": "MATCH (p:Person)-[:KNOWS]->(f) RETURN p.name, f.name LIMIT 5"},
)
```

## Notes

- The Query API accepts Cypher in a JSON payload and returns structured node/relationship arrays.
