---
title: 'Python SDK'
description: 'Async client for OAuth and managed-secret credentials. Tokens never leave the SDK.'
---

The Python SDK ships as the [`alter-sdk` package on PyPI](https://pypi.org/project/alter-sdk/). Python 3.10+.

```bash
pip install alter-sdk
```

```python
from alter_sdk import App, Agent, HttpMethod, Provider
```

## Two clients

The SDK exposes two top-level clients. They share the request surface but differ in principal and identity scoping.

<CardGroup cols={2}>
  <Card title="App" icon="building" href="/reference/python-sdk/client#app">
    Construct with an `alter_key_app_…` key. Acts as the application — provisions agents, mints Connect sessions, manages grants, and proxies provider calls under stored grants.
  </Card>
  <Card title="Agent" icon="robot" href="/reference/python-sdk/client#agent">
    Construct with an `alter_key_agent_…` key. Workload-scoped: can only reach credentials delegated or directly bound to the agent. The right entry point for AI agents and tool-calling workloads.
  </Card>
</CardGroup>

Both classes are async. Always close them (`await app.close()`) or use as an async context manager.

```python
async with App(api_key="alter_key_app_…") as app:
    page = await app.list_grants()
```

## Reference

<CardGroup cols={2}>
  <Card title="Client" icon="code" href="/reference/python-sdk/client">
    `App` and `Agent` constructors, lifecycle, `with_constraints`, `get_agent`.
  </Card>
  <Card title="request()" icon="paper-plane" href="/reference/python-sdk/request">
    Token-injected provider calls. Identity mode and direct grant mode.
  </Card>
  <Card title="proxy_request()" icon="shuffle" href="/reference/python-sdk/proxy">
    Backend-mediated provider calls. Use when HITL approval or server-side execution is required.
  </Card>
  <Card title="boto3_client()" icon="aws" href="/reference/python-sdk/aws">
    SigV4-bridged boto3 client for AWS APIs.
  </Card>
  <Card title="connect()" icon="link" href="/reference/python-sdk/connect">
    Headless OAuth flow, Connect sessions, polling, recovery sessions.
  </Card>
  <Card title="Grants" icon="key" href="/reference/python-sdk/grants">
    `list_grants`, `revoke_grant`, `create_managed_secret_grant`.
  </Card>
  <Card title="Agents namespace" icon="users" href="/reference/python-sdk/agents">
    `app.agents.create / list / get / update / delete`, agent-runtime tracing.
  </Card>
  <Card title="Keys namespace" icon="lock" href="/reference/python-sdk/keys">
    `keys.derive / rotate / revoke` — scoped sub-key lifecycle.
  </Card>
  <Card title="Approvals" icon="check" href="/reference/python-sdk/approvals">
    `await_approval`, `get_approval_status`, `PendingApproval`, `ApprovalResult`.
  </Card>
  <Card title="Scopes" icon="list" href="/reference/python-sdk/scopes">
    Scope catalog discovery.
  </Card>
  <Card title="Types" icon="cube" href="/reference/python-sdk/types">
    Pydantic models returned across the SDK surface.
  </Card>
  <Card title="Errors" icon="triangle-exclamation" href="/reference/python-sdk/errors">
    Typed exception hierarchy.
  </Card>
  <Card title="FastAPI" icon="bolt" href="/reference/python-sdk/frameworks/fastapi">
    `AlterFastAPI` dependency for automatic JWT capture.
  </Card>
  <Card title="LangChain" icon="link-simple" href="/reference/python-sdk/frameworks/langchain">
    `@alter_tool` decorator and `AlterMCPInterceptor`.
  </Card>
  <Card title="MCP" icon="plug" href="/reference/python-sdk/frameworks/mcp">
    `AlterMCP`, `AlterAuthProvider`, `AlterContext` for FastMCP servers.
  </Card>
</CardGroup>

## Configuration

The constructor accepts an `api_key` argument; application code is responsible for sourcing it. The convention is to read it from the process environment:

| Variable | Used by | Description |
|---|---|---|
| `ALTER_API_KEY` | application code (typical app workload) | Conventional name when the application owns one app-level key. |
| `AGENT_API_KEY` | application code (agent workload) | Conventional name when the workload runs as a managed agent. |

For agent workloads, the convention is to read `AGENT_API_KEY` in application code:

```python
import os
from alter_sdk import Agent

agent = Agent(api_key=os.environ["AGENT_API_KEY"])
```

## Conventions

- Every method is `async`. Use `await` and run inside an event loop.
- Models are frozen Pydantic objects. Fields are `snake_case` (matches the wire format).
- Methods that retrieve tokens never return the token plaintext. The SDK injects it into the outbound request.
- Public exceptions inherit from `AlterSDKError`. See [Errors](/reference/python-sdk/errors).
