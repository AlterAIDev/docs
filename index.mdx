---
title: 'Welcome to Alter Vault'
description: 'Secure OAuth token management for modern applications'
---

## Build Integrations in Minutes, Not Months

Alter Vault handles OAuth tokens and API credentials so you can focus on building your product. Connect your users to Google, Microsoft, Slack, GitHub, and dozens of other services without managing tokens, refresh logic, or security.

<CardGroup cols={2}>
  <Card
    title="Quickstart"
    icon="rocket"
    href="/quickstart"
  >
    Get up and running in under 5 minutes
  </Card>
  <Card
    title="Python SDK"
    icon="python"
    href="/sdks/python/installation"
  >
    Install and use our Python SDK
  </Card>
  <Card
    title="Alter Connect"
    icon="window"
    href="/sdks/javascript/installation"
  >
    Frontend SDK for OAuth connection UI
  </Card>
  <Card
    title="OpenClaw Plugin"
    icon="robot"
    href="/integrations/openclaw"
  >
    Give your AI agent OAuth access
  </Card>
</CardGroup>

## Why Choose Alter Vault?

### Never Store Tokens
Your database stays clean. We handle all token storage, encryption, and management in our secure infrastructure.

### Automatic Token Refresh
Tokens are refreshed automatically before they expire. Your API calls never fail due to expired credentials.

### Enterprise Security
Bank-grade encryption, SOC 2 compliance, and complete audit trails protect your users' data.

### Built for AI Agents
First-class actor tracking for AI agents and MCP servers. Know exactly which agent accessed what data and when, with per-request context (run ID, thread ID, tool call ID).

### Complete Audit Trail
Every token access, connection change, and API call is logged automatically. View, filter, and export logs from the dashboard for SOC 2, GDPR, and HIPAA compliance.

### Universal Compatibility
Works with your existing authentication system. Use JWT tokens from Auth0, Clerk, Supabase, or any provider.

## How It Works

<Steps>
  <Step title="Your User Connects">
    User authorizes access through our secure OAuth flow (Alter Connect UI)
  </Step>

  <Step title="We Store Tokens">
    Tokens are encrypted and stored in our secure vault - never in your database
  </Step>

  <Step title="You Make API Calls">
    Call `vault.request()` and we inject the right token automatically
  </Step>

  <Step title="We Handle the Rest">
    Token refresh, policy enforcement, audit logging - all automatic
  </Step>
</Steps>

## Our SDKs

<Info>
**Two SDKs for different purposes:**
- **Python SDK (`alter-sdk`)** - Backend SDK for making authenticated API calls with automatic token injection
- **Alter Connect (`@alter-ai/connect`)** - Frontend SDK for the OAuth connection UI
</Info>

## Quick Example

<CodeGroup>

```python Python
import asyncio
from alter_sdk import AlterVault, Provider, HttpMethod

async def use_google_calendar():
    vault = AlterVault(api_key="alter_key_...")

    # Make API request - token injected automatically!
    response = await vault.request(
        Provider.GOOGLE,
        HttpMethod.GET,
        "https://www.googleapis.com/calendar/v3/calendars/primary/events",
        user={"user_id": "alice"},
        query_params={"maxResults": "10"},
    )
    events = response.json()
    print(f"Found {len(events.get('items', []))} events")

    await vault.close()

asyncio.run(use_google_calendar())
```

```javascript JavaScript
import AlterConnect from '@alter-ai/connect';

// Create Connect instance
const alterConnect = AlterConnect.create({
    customization: {
        colors: { primary: '#6366f1' }
    }
});

// Open OAuth flow
async function connectProvider() {
    // Get session from YOUR backend
    const { session_token } = await fetch('/api/oauth/session')
        .then(r => r.json());

    await alterConnect.open({
        token: session_token,
        onSuccess: (connection) => {
            console.log('Connected!', connection);
        }
    });
}
```

```python Direct API
import requests

# Create session for frontend
response = requests.post(
    "https://api.alter.com/oauth/connect/session",
    headers={"x-api-key": "alter_key_..."},
    json={
        "end_user": {"id": "user_123"},
        "attributes": {"user_id": "user_123"}
    }
)
session_token = response.json()["session_token"]
```

</CodeGroup>

## Supported Providers

Connect to the services your users already use:

<CardGroup cols={4}>
  <Card title="Google" icon="google">
    Gmail, Calendar, Drive, Sheets
  </Card>
  <Card title="Microsoft" icon="microsoft">
    Outlook, Teams, OneDrive, Azure
  </Card>
  <Card title="Slack" icon="slack">
    Messages, Channels, Files
  </Card>
  <Card title="GitHub" icon="github">
    Repos, Issues, PRs, Actions
  </Card>
  <Card title="Salesforce" icon="cloud">
    CRM, Contacts, Opportunities
  </Card>
  <Card title="HubSpot" icon="hubspot">
    Contacts, Deals, Companies
  </Card>
  <Card title="Notion" icon="n">
    Pages, Databases, Workspaces
  </Card>
  <Card title="More" icon="plus">
    LinkedIn, Discord, Twitter, and more
  </Card>
</CardGroup>

## Built for Developers

<AccordionGroup>
  <Accordion title="Simple Integration">
    Drop-in SDK for Python with a single method (`vault.request()`) for any provider. REST API for everything else. Get started in minutes.
  </Accordion>

  <Accordion title="Your Auth System">
    Works with Auth0, Clerk, Supabase, Firebase, or any JWT-based authentication.
  </Accordion>

  <Accordion title="Complete Control">
    Fine-grained policies, audit logs, and actor tracking give you full visibility and control over who accesses what.
  </Accordion>

  <Accordion title="Scale Confidently">
    Handle millions of tokens with our infrastructure. Redis-cached token retrieval under 10ms.
  </Accordion>
</AccordionGroup>

## Getting Started

<Steps>
  <Step title="Sign Up">
    Create your free account at [portal.alter.com](https://portal.alter.com)
  </Step>

  <Step title="Create an App">
    Set up your first application in the Developer Portal
  </Step>

  <Step title="Configure Providers">
    Add OAuth credentials for Google, Slack, Microsoft, etc.
  </Step>

  <Step title="Install SDK">
    Add `alter-sdk` (Python) or `@alter-ai/connect` (JavaScript)
  </Step>

  <Step title="Connect Users">
    Use Alter Connect UI for OAuth authorization
  </Step>
</Steps>

## Support

<CardGroup cols={2}>
  <Card
    title="Documentation"
    icon="book"
    href="/quickstart"
  >
    Read our guides and tutorials
  </Card>
  <Card
    title="Audit Logs"
    icon="clipboard-list"
    href="/reference/audit-logs"
  >
    Compliance and observability
  </Card>
  <Card
    title="Architecture"
    icon="building"
    href="/reference/architecture"
  >
    Security model and design
  </Card>
  <Card
    title="Email Support"
    icon="envelope"
    href="mailto:support@alter.com"
  >
    Contact our support team
  </Card>
</CardGroup>
