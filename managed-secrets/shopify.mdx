---
title: 'Shopify'
description: 'Connect Shopify to Alter Vault for secure API access'
---

## Overview

Use Shopify credentials to make authenticated API calls through Alter Vault without exposing your API keys in code.

| Property | Value |
|----------|-------|
| **Provider ID** | `shopify` |
| **Category** | E-Commerce |
| **Credential Type** | API Key |

## Step 1: Get Your Credentials

<Steps>
  <Step title="Log in to your Shopify Admin">
    Log in to your Shopify Admin at [admin.shopify.com](https://admin.shopify.com/).
  </Step>
  <Step title="Go to Settings → Apps">
    Go to Settings → Apps and sales channels → Develop apps.
  </Step>
  <Step title="Create an app">
    Click 'Create an app', configure API scopes, and install it.
  </Step>
  <Step title="Copy the Admin API access token">
    Copy the Admin API access token (starts with shpat_).
  </Step>
  <Step title="Enter your store subdomain">
    Enter your store subdomain (e.g. 'your-store' from your-store.myshopify.com) in the Store Subdomain field.
  </Step>
</Steps>

<Warning>
  Never share your credentials publicly. Store them securely and only enter them in the Alter Vault Developer Portal.
</Warning>

## Step 2: Add to Alter Vault

<Steps>
  <Step title="Open the Developer Portal">
    Go to [portal.alterai.dev](https://portal.alterai.dev) and navigate to your app.
  </Step>

  <Step title="Add Shopify">
    Go to **Managed Secrets** > **Add Provider** > **Shopify**.
  </Step>

  <Step title="Enter your credentials">
    Paste your Admin API Access Token into the API Key field, and enter your store subdomain (e.g. `your-store`) in the Store Subdomain field.
  </Step>

  <Step title="Save">
    Click **Save**. You'll receive a `connection_id` to use with the SDK.
  </Step>
</Steps>

## Using in Code

```python
response = await vault.request(
    "YOUR_CONNECTION_ID",
    HttpMethod.GET,
    "https://your-store.myshopify.com/admin/api/YOUR_API_VERSION/products.json",
)
```
